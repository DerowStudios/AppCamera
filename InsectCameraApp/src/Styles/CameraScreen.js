import { StyleSheet } from "react-native";

const CameraScreen = StyleSheet.create({
  image: {
    width: 200,
    height: 200,
    marginVertical: 20,
  },
  detailsContainer: {
    marginVertical: 10,
    padding: 10,
    borderWidth: 1,
    borderColor: "#ddd",
    borderRadius: 5,
  },
});

export default CameraScreen;
