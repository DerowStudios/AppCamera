import { StyleSheet } from "react-native";

const LateralMenu = StyleSheet.create({
  screenDisplay: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#e8eaf6",
  },
});

export default LateralMenu;
